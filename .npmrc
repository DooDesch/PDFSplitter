# Retry and timeout settings for unstable network (e.g. pnpm install)
# 20 retries with short intervals between attempts
fetch-retries=20
fetch-retry-mintimeout=500
fetch-retry-maxtimeout=1000
fetch-retry-factor=2
fetch-timeout=5000
